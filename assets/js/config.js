/* NEXT COP Readiness Board — deployment configuration
 *
 * This is the only front-end file you need to edit. Everything else in
 * docs/ can be left exactly as committed.
 *
 * Credit partner    : Industry Compliance & Sustainability Platform
 * Technology partner: guulba — technology for better performance
 */

window.BOARD_CONFIG = {

  /* Paste the Apps Script web-app /exec URL here to turn on Sync to Sheet.
     Leave it empty and the board runs fully offline — Export JSON and
     Import JSON still work. See apps-script/README.md for the deployment. */
  appsScriptUrl: "",

  /* Shown in the left rail under the board name. Use it to name the site
     this board belongs to, e.g. "Unit 2 — Gazipur". */
  siteName: "",

  /* Partner credits shown in the rail and the page footer. */
  creditPartner:     "Industry Compliance & Sustainability Platform",
  technologyPartner: "guulba — technology for better performance",

  /* Timezone used for date stamps on updates and closures. */
  timezone: "Asia/Dhaka"
};
